export interface LeaderboardType {
  name: string,
  totalPoints: number,
  totalGames: number,
  totalVictories: number,
  totalDraws: number,
  totalLosses: number,
  totalFavor: number,
  totalOwn: number,
}
